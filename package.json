{
  "name": "puppeteer-screenshots",
  "version": "1.0.0",
  "description": "Use Puppeteer to automate screenshots of url based on common device widths",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mukesh Kumar",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^0.12.0"
  }
}
